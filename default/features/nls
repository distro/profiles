Features::Default.define('nls') {
  description 'Adds Native Language Support (using gettext - GNU locale utilities)'

  before :dependencies do |deps|
    deps << 'system/development/gettext' if enabled?
  end
}
