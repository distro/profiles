Features::Default.define('gdbm') {
  description 'Adds support for system/library/database/gnu (GNU database libraries)'

  before :dependencies do |deps|
    deps << 'system/library/database/gnu' if enabled?
  end
}
