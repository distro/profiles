Features::Default.define('gpm') {
  description 'Adds support for system/library/gpm (Console-based mouse driver)'

  before :dependencies do |deps|
    deps << 'system/library/gpm' if enabled?
  end
}
