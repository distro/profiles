Features::Default.define('ruby') {
  description 'Adds support/bindings for the Ruby language'

  before :dependencies do |deps|
    deps << 'application/development/interpreter/ruby' if enabled?
  end
}
