Features::Default.define('gmp') {
  description 'Adds support for development/library/gmp (GNU MP library)'

  before :dependencies do |deps|
    deps << 'development/library/gmp' if enabled?
  end
}
