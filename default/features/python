Features::Default.define('python') {
  description 'Adds support/bindings for the Python language'

  before :dependencies do |deps|
    deps << 'application/development/interpreter/python' if enabled?
  end
}
