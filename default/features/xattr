Features::Default.define('xattr') {
  description 'Adds support for extended attributes (filesystem-stored metadata)'

  before :dependencies do |deps|
    deps << 'system/application/attr' if enabled?
  end
}
