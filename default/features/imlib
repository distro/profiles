Features::Default.define('imlib') {
  description 'Adds support for imlib, an image loading and rendering library'

  before :dependencies do |deps|
    deps << 'media/library/imlib2' if enabled?
  end
}
