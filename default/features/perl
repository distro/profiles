Features::Default.define('perl') {
  description 'Adds support/bindings for the Perl language'

  before :dependencies do |deps|
    deps << 'application/development/interpreter/perl' if enabled?
  end
}
